instaloader
requests
curl_cffi
python-dateutil
pytz
tzlocal
python-dotenv
tqdm
rich
flask
jinja2
colorama; platform_system == "Windows"

# Optional: uncomment only for display-width-aware terminal truncation (TRUNCATE_CHARS, --truncate)
# wcwidth

# Optional: uncomment only to import Chrome/Brave/Chromium sessions (--browser chrome|brave|chromium); macOS and Linux only
# pycookiecheat
